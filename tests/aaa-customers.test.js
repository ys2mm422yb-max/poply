import test from 'node:test';import assert from 'node:assert/strict';import { readFile } from 'node:fs/promises';import { createInitialState } from '../src/v2-game.js';import { CUSTOMER_ART, customerArtUrl } from '../src/aaa-customers.js';import { GUEST_PROFILES, guestForSequence, ensureGuestState, guestLoyalty, recordGuestService, totalGuestVisits } from '../src/aaa-guests.js';import { ORDER_CHOICE_POLISH_CSS } from '../src/aaa-order-choice-polish.js';
const root=new URL('../',import.meta.url),read=p=>readFile(new URL(p,root),'utf8');
test('customer art exposes three original vector portraits',()=>{assert.equal(CUSTOMER_ART.length,3);for(const art of CUSTOMER_ART){assert.match(art,/^data:image\/svg\+xml/);assert.match(decodeURIComponent(art),/<svg/);assert.match(decodeURIComponent(art),/viewBox="0 0 120 120"/);}assert.equal(customerArtUrl(0),CUSTOMER_ART[0]);assert.equal(customerArtUrl(4),CUSTOMER_ART[1]);});
test('guest identity follows deterministic portrait sequence',()=>{assert.deepEqual(GUEST_PROFILES.map(g=>g.id),['mika','nora','sam']);assert.equal(guestForSequence(0).id,'mika');assert.equal(guestForSequence(3).id,'mika');});
test('guest loyalty rewards exact real-visit milestones',()=>{let state=ensureGuestState(createInitialState()).state;assert.deepEqual(state.guestVisits,{mika:0,nora:0,sam:0});let result=recordGuestService(state,0);state=result.state;assert.equal(result.rewardCoins,25);assert.equal(state.coins,125);assert.equal(guestLoyalty(state,'mika').title,'Bekannt');for(let i=0;i<3;i++)state=recordGuestService(state,0).state;result=recordGuestService(state,0);state=result.state;assert.equal(result.visits,5);assert.equal(result.rewardCoins,100);assert.equal(result.milestone.title,'Stammgast');assert.equal(state.coins,225);assert.equal(totalGuestVisits(state),5);assert.equal(ensureGuestState(structuredClone(state)).changed,false);});
test('legacy saves do not invent retroactive guest rewards',()=>{const legacy=createInitialState();legacy.stats.orders=25;legacy.coins=777;const result=ensureGuestState(legacy);assert.deepEqual(result.state.guestVisits,{mika:0,nora:0,sam:0});assert.equal(result.state.coins,777);});
test('active AAA order views no longer import legacy customer webps',async()=>{const view=await read('src/aaa-view.js');assert.match(view,/aaa-customers\.js/);assert.match(view,/customerArtUrl/);assert.doesNotMatch(view,/v2-customer-[abc]\.js/);});
test('Collection exposes compact guest loyalty without a new main navigation tab',async()=>{const [view,css]=await Promise.all([read('src/aaa-collection-view.js'),read('src/aaa-guests.css')]);assert.match(view,/collection-guests/);assert.match(view,/STAMMGÄSTE/);assert.match(view,/guestLoyalty/);assert.match(css,/\.guest-summary-row/);assert.match(css,/@media\(max-height:740px\)/);assert.doesNotMatch(view,/nav-guest|data-view="guests"/);});
test('Orders service stage uses interaction-safe authored light and distinct reward colors',async()=>{const css=await read('src/aaa-service.css');assert.match(css,/Orders vibrance pass/);assert.match(css,/\.service-card:after\{[^}]*pointer-events:none[^}]*animation:service-stage-breathe/s);assert.match(css,/\.service-rewards>span:first-child\{/);assert.match(css,/\.service-rewards>span:nth-child\(2\)\{/);assert.match(css,/@keyframes service-stage-breathe/);assert.match(css,/@media\(prefers-reduced-motion:reduce\)\{\.service-card:after\{animation:none!important;transform:none!important\}\}/);});
test('compact Orders guest choices preserve two readable title lines without growing queue',async()=>{const main=await read('src/aaa-main.js');assert.match(main,/aaa-order-choice-polish\.js/);assert.match(ORDER_CHOICE_POLISH_CSS,/\.customer-choice strong/);assert.match(ORDER_CHOICE_POLISH_CSS,/white-space:normal/);assert.match(ORDER_CHOICE_POLISH_CSS,/-webkit-line-clamp:2/);});
test('Orders counter polish stays interaction-safe',async()=>{const [html,css]=await Promise.all([read('index.html'),read('src/aaa-orders-counter.css')]);assert.ok(html.indexOf('./src/aaa-orders-counter.css')>html.indexOf('./src/aaa.css'));assert.match(css,/pointer-events:none/);assert.match(css,/@media\(prefers-reduced-motion:reduce\)/);});
