{
  "name": "poply",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test && node --check src/main.js && node --check src/polish.js",
    "check": "npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
